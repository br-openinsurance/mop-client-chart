{{- if .Values.mopGateway.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mop-gateway
  namespace: {{ .Values.mopGateway.namespace }}
  labels:
    app: mop-gateway
    {{- include "mop-client.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.mopGateway.replicaCount }}
  selector:
    matchLabels:
      app: mop-gateway
  template:
    metadata:
      labels:
        app: mop-gateway
        {{- include "mop-client.labels" . | nindent 8 }}
    spec:
      {{- if .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.global.imagePullSecrets | nindent 8 }}
      {{- end }}

      containers:
        - name: mop-gateway
          image: "{{ .Values.global.imageRegistry }}/{{ .Values.mopGateway.image.repository }}:{{ .Values.mopGateway.image.tag }}"
          imagePullPolicy: {{ .Values.mopGateway.image.pullPolicy }}

          ports:
            - name: http
              containerPort: {{ .Values.mopGateway.service.port }}

          env:
            - name: MOP_SERVER_URL
              value: "{{ .Values.global.mopServerUrl }}"
            {{- range $key, $value := .Values.mopGateway.env }}
            - name: {{ $key }}
              value: "{{ $value }}"
            {{- end }}

          resources:
            {{- toYaml .Values.mopGateway.resources | nindent 12 }}
{{- end }}
